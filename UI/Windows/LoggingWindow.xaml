<Window x:Class="WindowsSipPhone.UI.Windows.LoggingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"        Title="📋 Application Logging &amp; Diagnostics" 
        Height="700" 
        Width="1000"
        WindowStartupLocation="Manual"
        Background="#F8F9FA">
    
    <Window.Resources>
        <!-- Modern Styles -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        
        <Style x:Key="SubHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#BDC3C7"/>
            <Setter Property="Margin" Value="0,3,0,0"/>
        </Style>
        
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                               CornerRadius="4" 
                               BorderBrush="{TemplateBinding BorderBrush}" 
                               BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#3498DB" CornerRadius="5" Padding="20,15,20,15" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="📋 Application Logging &amp; Diagnostics" Style="{StaticResource HeaderStyle}"/>
                <TextBlock Text="Real-time application logging and diagnostic information" Style="{StaticResource SubHeaderStyle}"/>
            </StackPanel>
        </Border>
        
        <!-- Toolbar -->
        <ToolBar Grid.Row="1" Margin="0,0,0,15" Background="White" BorderBrush="#E0E0E0" BorderThickness="1">
            <Button x:Name="StartLoggingButton" Content="🟢 Start Logging"
                    ToolTip="Start capturing application logs"
                    Background="#27AE60" 
                    Style="{StaticResource ActionButtonStyle}"/>
            
            <Button x:Name="StopLoggingButton" Content="🔴 Stop Logging"
                    ToolTip="Stop capturing application logs"
                    Background="#E74C3C" 
                    Style="{StaticResource ActionButtonStyle}"/>
            
            <Separator/>
            
            <Button x:Name="ClearLogsButton" Content="🗑️ Clear"
                    ToolTip="Clear all log entries"
                    Background="#95A5A6" 
                    Style="{StaticResource ActionButtonStyle}"/>
            
            <Button x:Name="SaveLogsButton" Content="💾 Save"
                    ToolTip="Save logs to file"
                    Background="#3498DB" 
                    Style="{StaticResource ActionButtonStyle}"/>
            
            <Separator/>
            
            <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="10,0,5,0" FontWeight="Bold"/>
            <ComboBox x:Name="LogLevelFilter" Width="100" SelectedIndex="0">
                <ComboBoxItem Content="All"/>
                <ComboBoxItem Content="Debug"/>
                <ComboBoxItem Content="Info"/>
                <ComboBoxItem Content="Warning"/>
                <ComboBoxItem Content="Error"/>
            </ComboBox>
            
            <CheckBox x:Name="AutoScrollCheckBox" Content="📜 Auto-scroll" 
                      IsChecked="True" VerticalAlignment="Center" Margin="15,0,0,0"/>
        </ToolBar>
        
        <!-- Log Content -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Main Log View -->
            <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="3">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Log Header -->
                    <Border Grid.Row="0" Background="#ECF0F1" Padding="10,8" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" Text="📋 Application Logs" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" x:Name="LogCountText" Text="0 entries" FontSize="11" Foreground="#7F8C8D" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    
                    <!-- Log Entries -->
                    <ScrollViewer Grid.Row="1" x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto">
                        <ListView x:Name="LogListView" BorderThickness="0" SelectionMode="Single">                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Time" Width="80" DisplayMemberBinding="{Binding FormattedTimestamp}"/>
                                    <GridViewColumn Header="Level" Width="60" DisplayMemberBinding="{Binding Level}"/>
                                    <GridViewColumn Header="Source" Width="120" DisplayMemberBinding="{Binding Source}"/>
                                    <GridViewColumn Header="Message" Width="400" DisplayMemberBinding="{Binding Message}"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </ScrollViewer>
                </Grid>
            </Border>
            
            <!-- Splitter -->
            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="#BDC3C7"/>
            
            <!-- Stats and Info Panel -->
            <Border Grid.Column="2" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="3">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="15">
                        <!-- Logging Status -->
                        <Border Background="#ECF0F1" Padding="10" CornerRadius="3" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="📊 Status" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Status:" FontWeight="Bold" Margin="0,2"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="LoggingStatusText" Text="Inactive" Margin="5,2" Foreground="#E74C3C"/>
                                    
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Total:" FontWeight="Bold" Margin="0,2"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TotalLogsText" Text="0" Margin="5,2"/>
                                    
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Filtered:" FontWeight="Bold" Margin="0,2"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="FilteredLogsText" Text="0" Margin="5,2"/>
                                    
                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Rate:" FontWeight="Bold" Margin="0,2"/>
                                    <TextBlock Grid.Row="3" Grid.Column="1" x:Name="LogRateText" Text="0/min" Margin="5,2"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                        
                        <!-- Log Level Distribution -->
                        <Border Background="#ECF0F1" Padding="10" CornerRadius="3" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="📈 Distribution" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Debug:" FontWeight="Bold" Margin="0,2"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="DebugCountText" Text="0" Margin="5,2" Foreground="#95A5A6"/>
                                    
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Info:" FontWeight="Bold" Margin="0,2"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="InfoCountText" Text="0" Margin="5,2" Foreground="#3498DB"/>
                                    
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Warning:" FontWeight="Bold" Margin="0,2"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="WarningCountText" Text="0" Margin="5,2" Foreground="#F39C12"/>
                                    
                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Error:" FontWeight="Bold" Margin="0,2"/>
                                    <TextBlock Grid.Row="3" Grid.Column="1" x:Name="ErrorCountText" Text="0" Margin="5,2" Foreground="#E74C3C"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                        
                        <!-- Quick Actions -->
                        <Border Background="#ECF0F1" Padding="10" CornerRadius="3">
                            <StackPanel>
                                <TextBlock Text="⚡ Quick Actions" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                                <Button Content="🔄 Refresh" Background="#3498DB" Style="{StaticResource ActionButtonStyle}" Margin="0,2"/>
                                <Button Content="📊 Generate Report" Background="#27AE60" Style="{StaticResource ActionButtonStyle}" Margin="0,2"/>
                                <Button Content="🔧 Open Log Folder" Background="#95A5A6" Style="{StaticResource ActionButtonStyle}" Margin="0,2"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Window>
